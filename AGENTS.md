# 仓库协作说明

## 项目定位

本仓库维护 `game-video-editor` Codex 技能，用于分析游戏录像、筛选候选片段、制定带时间码的剪辑清单，并优先为 Premiere 生成源时间线粗剪；FFmpeg、剪映等工具作为按需交接或备用路径。

当前重点是《蔚蓝》视频，以及以下两类实验风格：

- 美式鬼畜：高密度笑点、快速节奏、少量吐槽或自嘲。
- 结构化冷幽默：保留挑战过程和情绪递进，以克制包装突出最终胜利。

## 新对话交接上下文

在新的 Codex 对话中开始工作时，先阅读本文件、`SKILL.md`，再按任务读取相关的 `references/*.md`。以下内容是已经确定的项目背景，不要要求用户重新解释：

- 用户准备把本仓库作为独立项目长期维护；实际仓库位于 `E:\kaifa\skills\codex-skill-repos\game-video-editor`。
- GitHub 远程仓库为公开仓库 `WL-tea/game-video-editor`。当前不急于添加许可证，等技能较完善后再决定。
- 目标是用 AI 为视频创作铺设可追溯、可调整的地基：自动完成素材覆盖、候选裁切与命名、时间码映射、公式化粗剪和可删除建议；独特创意、审美节奏和最终验收由用户决定，默认由用户在 AI 粗剪上继续精剪。
- 采用人机协作剪辑。AI 必须披露覆盖范围、未采用的高价值候选和舍弃理由；具体确认点、交付模式和精剪范围以 `references/editing-workflow.md` 为唯一规范。
- Premiere XML 时间线被定位为《蔚蓝》默认粗剪审片和用户细剪交接方式；FFmpeg 只用于用户明确需要媒体文件或 XML 路径不可用的备用场景。Premiere、剪映等工具可承担复杂包装和人工精修。
- 技能按“游戏配置＋风格配置”组织，而不是为每种风格拆成独立技能。只有工作流或工具链明显分化时才考虑拆分。
- 第一个实验游戏是《蔚蓝》。素材可能缺少口播、台词和摄像头，因此幽默主要来自游戏事件、字幕、音效、重复、计数、节奏和回收梗。
- 目标平台当前以哔哩哔哩为主，账号处于新人试水阶段。关于新人流量或某种风格效果的判断都只是待验证假设，不得伪装成已有数据结论。
- 用户不希望过早、过量塑造创作者人格。可以使用少量情境吐槽或自嘲，但不突出个人宣言、自我介绍或“强行立人设”。
- 首轮计划对比两种方案：A 为纯粹美式鬼畜加少量吐槽或自嘲；B 为事件主导的结构化冷幽默。先做多个样本，再根据留存、互动、制作成本和定性反馈迭代。
- 当前仓库已经完成技能骨架、中文化、人机协作剪辑工作流和 Premiere XML 粗剪交接规则，但尚未用真实《蔚蓝》长录像完成一次端到端验证。下一阶段应优先使用恒定帧率真实素材检验覆盖记录、候选确认、每源文件 XML 导入、轨道分层、标记追溯、局部修改和风格复盘流程。

交接信息只记录稳定决策。已经写入 `SKILL.md` 或 `references/` 的操作细节不要在这里重复；新实验产生结论后，将稳定规则写入对应配置，并同步更新本节中的项目状态或未决事项。

## 目录职责

- `SKILL.md`：技能入口、触发范围和通用工作流程。
- `references/editing-workflow.md`：人机协作剪辑的端到端执行流程、确认节点和产物格式。
- `references/celeste.md`：游戏专属知识与素材判断规则。
- `references/american-meme.md`：美式鬼畜风格规则。
- `references/structured-deadpan.md`：结构化冷幽默风格规则。
- `references/style-experiment.md`：风格实验、对照方法和复盘指标。
- `references/premiere-xml.md`：Premiere XML 粗剪和细剪交接、源时间线、轨道分层、标记与验证规则。
- `agents/openai.yaml`：技能展示与代理元数据。

新增游戏或风格时，优先添加独立的 `references/*.md` 配置，并在 `SKILL.md` 中建立清晰入口；不要把所有细节堆入主文件。

## 编写规则

- 面向用户的说明、剪辑方案和规则默认使用简体中文。
- 文件名、命令、代码、软件名称和必要的专有名词可以保留英文。
- 规则应当具体、可执行，尽量给出判断条件、输入输出和检查方法。
- 将游戏事实、剪辑风格和执行工具分层，避免相互绑定。
- 不把未经验证的平台流量结论写成确定事实；经验判断需标明是假设或实验方向。
- 不默认加入大量人格表达。新人视频以素材和笑点为主，吐槽、自嘲应克制并服务节奏。
- 外部视频研究只用于提炼可执行规则；仓库文件不记录具体视频名称、作者、链接、账号、播放数据、观看清单或逐条研究过程。
- 不提交原始录像、音频、导出视频、缓存、临时工作目录或敏感信息。
- 每次完善 `SKILL.md` 或 `references/*.md` 时，在不损失约束精度的前提下优先使用最短表达：删除重复背景、重复解释和无法改变执行结果的修辞；已有规则只引用其唯一规范，不在多个文件中全文复制。
- 每条会影响 AI 行为的规则明确“何时触发、执行什么、产出什么、何时停止或例外”；避免单独使用“适当”“尽量”“必要时”“等”这类没有判断标准的词，也不要把示例写成容易误读的隐含硬约束。
- 完成修改前进行一次歧义和上下文检查：确认规则之间没有冲突，适用范围没有遗漏，例外不会被误读为默认行为；将稳定背景放在本文件，将通用入口放在 `SKILL.md`，将具体操作放在对应 `references/*.md`，控制每次任务需要阅读的内容。

## 修改与验证

修改后至少完成以下检查：

1. 确认 `SKILL.md` 的 YAML 元数据仍然有效，名称与目录一致。
2. 检查新增引用文件能够从 `SKILL.md` 或相关配置中找到。
3. 检查中文表达、标题层级、相对路径和 Markdown 格式。
4. 运行仓库已有的测试或验证命令；如果没有自动化测试，至少检查 `git diff --check`。
5. 用 `git status` 确认没有误加入视频、音频、缓存或输出文件。

如果本次修改改变了项目定位、已经确认的用户偏好、实验阶段或下一步重点，同时更新“新对话交接上下文”，保证后续对话不会依据过期状态工作。

## Git 协作

- 保留用户已有的修改，不覆盖或回退无关内容。
- 提交应聚焦单一目的，提交信息应清楚说明改动。
- 未经用户明确确认，不执行 `git push`、删除远程分支、创建 Pull Request，或进行其他会修改 GitHub 远程状态的操作。
- 本地检查、暂存和本地提交可以按用户要求执行；是否提交仍以当前任务授权为准。
