---
name: game-video-editor
description: 用于需要人机协作分析游戏录像、审查候选素材、制定带时间码且可由用户确认的剪辑方案、执行或修改粗剪、比较剪辑风格，或为 FFmpeg、Premiere、剪映等工具准备可追溯剪辑交付的场景；当前包含《蔚蓝》游戏配置以及美式鬼畜、结构化冷幽默风格配置。
---

# 游戏视频剪辑

将原始游戏录像转化为可审查、可修改且可追溯的剪辑方案或成片草稿。把游戏特有的素材判断与视频包装风格分开，先用真实投稿验证风格，再逐步固化规则。

默认使用中文输出；用户明确要求其他语言时再切换。保留软件名称、命令、文件名和必要的专有名词原文。

## 协作原则

默认采用人机协作，不假定用户会把完整剪辑任务交给 AI：

- AI 负责扩大素材观察范围、记录事件、整理候选、提出方案、执行已确认的机械操作并维护版本记录；
- 用户负责精彩程度、叙事方向、关键节奏、例外片段和最终成品的判断；
- 除非用户明确授权跳过，否则在候选素材、剪辑方向、粗剪和最终成品四个节点等待用户确认；
- 对已获确认、可逆且不会改变创作方向的操作连续执行，不要把流程切碎成逐刀确认；
- 展示未采用的高价值候选及舍弃理由，不要只交付 AI 自行筛出的最终片段；
- 每版粗剪保留成片时间码与原素材时间码的映射，使用户能够定位遗漏、恢复片段或提出局部修改；
- 用户未验收的版本称为草稿或候选成片，不称为最终成片。

## 选择配置

1. 需要审片、选材、制定方案、执行剪辑、修改草稿或验收成片时，读取 [references/editing-workflow.md](references/editing-workflow.md)。
2. 确认游戏、目标平台、目标时长、画面比例、可用音轨和剪辑风格。
3. 读取对应游戏配置。处理《蔚蓝》时，读取 [references/celeste.md](references/celeste.md)。
4. 只读取本次需要的一种风格配置：
   - 高密度美式鬼畜：[references/american-meme.md](references/american-meme.md)
   - 事件主导的结构化冷幽默：[references/structured-deadpan.md](references/structured-deadpan.md)
5. 比较风格或规划试水系列时，再读取 [references/style-experiment.md](references/style-experiment.md)。
6. 没有匹配配置时，根据素材建立临时配置并明确标注假设，不要把某一款游戏的规则当作通用规则。

## 执行要求

遵循端到端工作流维护素材覆盖记录、事件日志、候选片段表、剪辑决策表和成片映射表。没有实际访问素材时，不得声称已经观看或分析；无法完整检查时，明确报告尚未覆盖的范围。

选择合适的工具执行已确认的操作：

- 使用可复现的命令完成裁切、拼接、缩放、响度处理、简单字幕和导出；
- 使用 Premiere、剪映等非线性剪辑软件完成复杂动画、跟踪、蒙版和人工节奏调整；
- 不修改原始素材，草稿和最终候选写入不同路径；
- 成本较高的完整导出前先生成短预览，局部意见优先生成局部修改预览；
- 区分已完成的自动处理、尚需人工完成的步骤和等待用户确认的决策。

不要加入未经验证的自动化脚本。只有同一种操作反复出现并经过真实素材测试后，才将其固化为脚本。

## 内容原则

应用选定的游戏与风格配置，并默认弱化创作者的显性人格表达：

- 让游戏事件成为主体；
- 优先使用情境字幕，少用自我介绍和个人宣言；
- 让笑点逐步升级，不要从头到尾维持同一强度；
- 用效果澄清或放大既有事件；
- 保护困难成功所带来的情绪回报。

素材没有口播时，使用简短字幕、计数器、重复视觉元素、音乐递进和回收梗建立连续性，不要用无关梗填满每一帧。

## 审核底线

检查：

- 开头几秒是否明确呈现事件、问题或结果悬念；
- 观众能否识别当前目标；
- 重复失败是否被压缩或形成升级；
- 字幕是否可读且没有过度解释；
- 对话和关键游戏声音是否清楚；
- 最终成功是否容易理解并具有情绪回报；
- 画面比例、安全区域、响度和导出设置是否匹配平台；
- 成片是否真正采用选定配置，而不是未经要求的混合风格；
- 每个成片片段是否能追溯到原素材，用户反馈是否已记录为已处理、保留或待确认。

进行风格实验时，同时记录制作时间和观众数据。不要根据一次投稿宣布某种风格胜出。

## 扩展技能

新增游戏或风格时，在 `references/` 下增加一级参考文件，并从本文件直接链接。只有工具、素材分析方法和执行流程出现明显分化时，才拆分为独立技能。
